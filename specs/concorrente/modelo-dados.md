# Modelo de dados — concorrente

## Origem

Arquivos `.tll` em `migration-source/infotime-migration/concorrente/database/`.

## Tabelas


### `concorrente.tll`

```sql
-- Tabela: concorrente
-- Entidade: concorrente
-- Origem: MySQL → PostgreSQL (schema liga_infotime)

-- liga_infotime.concorrente  (origem MySQL: concorrente)
CREATE TABLE concorrente (
  id_concorrente bigint GENERATED BY DEFAULT AS IDENTITY NOT NULL,
  id_tenacidade bigint DEFAULT NULL,
  id_usuario_auditoria bigint DEFAULT NULL,
  nome_fantasia varchar(255) DEFAULT NULL,
  nome_sistema varchar(100) DEFAULT NULL,
  cidade varchar(100) DEFAULT NULL,
  estado char(2) DEFAULT NULL,
  telefone varchar(50) DEFAULT NULL,
  celular varchar(50) DEFAULT NULL,
  contato varchar(100) DEFAULT NULL,
  email varchar(100) DEFAULT NULL,
  home_page varchar(255) DEFAULT NULL,
  diferenciais text,
  observacoes text,
  endereco_ip_auditoria varchar(50) DEFAULT NULL,
  nome_aplicacao_auditoria varchar(255) DEFAULT NULL,
  PRIMARY KEY (id_concorrente)
);
```

## Prisma (próximos passos)

- Converter para models no pacote `packages/database` seguindo [`docs/PADRAO_PRISMA.md`](../../docs/PADRAO_PRISMA.md).
- Preservar nomes físicos com `@@map` / `@map`.
