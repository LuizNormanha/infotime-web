# Modelo de dados — colaborador-tarefa

## Origem

Arquivos `.tll` em `migration-source/infotime-migration/colaborador-tarefa/database/`.

## Tabelas


### `colaborador_plano_conta.tll`

```sql
-- Tabela: colaborador_plano_conta
-- Entidade: colaborador-tarefa
-- Origem: MySQL → PostgreSQL (schema liga_infotime)

-- liga_infotime.colaborador_plano_conta  (origem MySQL: colaboradorplanoconta)
CREATE TABLE colaborador_plano_conta (
  id_colaborador_plano_conta bigint GENERATED BY DEFAULT AS IDENTITY NOT NULL,
  id_tenacidade bigint DEFAULT NULL,
  id_colaborador bigint DEFAULT NULL,
  id_plano_conta bigint DEFAULT NULL,
  id_usuario_auditoria bigint DEFAULT NULL,
  endereco_ip_auditoria varchar(50) DEFAULT NULL,
  nome_aplicacao_auditoria varchar(255) DEFAULT NULL,
  PRIMARY KEY (id_colaborador_plano_conta)
);
```

### `colaborador_tarefa.tll`

```sql
-- Tabela: colaborador_tarefa
-- Entidade: colaborador-tarefa
-- Origem: MySQL → PostgreSQL (schema liga_infotime)

-- liga_infotime.colaborador_tarefa  (origem MySQL: colaboradortarefa)
CREATE TABLE colaborador_tarefa (
  id_colaborador_tarefa bigint GENERATED BY DEFAULT AS IDENTITY NOT NULL,
  id_tenacidade bigint DEFAULT NULL,
  id_colaborador bigint DEFAULT NULL,
  id_contrato bigint DEFAULT NULL,
  id_usuario_auditoria bigint DEFAULT NULL,
  descricao varchar(255) DEFAULT NULL,
  data_inicio timestamp without time zone DEFAULT NULL,
  data_previsao timestamp without time zone DEFAULT NULL,
  data_previsao_inicial timestamp without time zone DEFAULT NULL,
  data_conclusao timestamp without time zone DEFAULT NULL,
  prioridade char(1) DEFAULT NULL,
  situacao char(1) DEFAULT NULL,
  qtd_alteracao_data smallint DEFAULT NULL,
  observacoes text,
  endereco_ip_auditoria varchar(50) DEFAULT NULL,
  nome_aplicacao_auditoria varchar(255) DEFAULT NULL,
  PRIMARY KEY (id_colaborador_tarefa)
);
```

### `colaborador_tarefa_historico.tll`

```sql
-- Tabela: colaborador_tarefa_historico
-- Entidade: colaborador-tarefa
-- Origem: MySQL → PostgreSQL (schema liga_infotime)

-- liga_infotime.colaborador_tarefa_historico  (origem MySQL: colaboradortarefahistorico)
CREATE TABLE colaborador_tarefa_historico (
  id_colaborador_tarefa_historico bigint GENERATED BY DEFAULT AS IDENTITY NOT NULL,
  id_colaborador_tarefa bigint DEFAULT NULL,
  id_colaborador_tarefa_motivo_prorrogacao bigint DEFAULT NULL,
  id_usuario bigint DEFAULT NULL,
  data_hora timestamp without time zone DEFAULT NULL,
  PRIMARY KEY (id_colaborador_tarefa_historico)
);
```

### `colaborador_tarefa_motivo_prorrogacao.tll`

```sql
-- Tabela: colaborador_tarefa_motivo_prorrogacao
-- Entidade: colaborador-tarefa
-- Origem: MySQL → PostgreSQL (schema liga_infotime)

-- liga_infotime.colaborador_tarefa_motivo_prorrogacao  (origem MySQL: colaboradortarefamotivoprorrogacao)
CREATE TABLE colaborador_tarefa_motivo_prorrogacao (
  id_colaborador_tarefa_motivo_prorrogacao bigint GENERATED BY DEFAULT AS IDENTITY NOT NULL,
  id_tenacidade bigint DEFAULT NULL,
  id_usuario_auditoria bigint DEFAULT NULL,
  nome varchar(255) DEFAULT NULL,
  endereco_ip_auditoria varchar(50) DEFAULT NULL,
  nome_aplicacao_auditoria varchar(255) DEFAULT NULL,
  PRIMARY KEY (id_colaborador_tarefa_motivo_prorrogacao)
);
```

## Prisma (próximos passos)

- Converter para models no pacote `packages/database` seguindo [`docs/PADRAO_PRISMA.md`](../../docs/PADRAO_PRISMA.md).
- Preservar nomes físicos com `@@map` / `@map`.
