# Modelo de dados — auth

## Origem

Arquivos `.tll` em `migration-source/infotime-migration/auth/database/`.

## Tabelas


### `webcam.tll`

```sql
-- Tabela: webcam
-- Entidade: auth
-- Origem: MySQL → PostgreSQL (schema liga_infotime)

-- liga_infotime.webcam  (origem MySQL: webcam)
CREATE TABLE webcam (
  id integer GENERATED BY DEFAULT AS IDENTITY NOT NULL,
  nome varchar(255) DEFAULT NULL,
  imagem bytea,
  PRIMARY KEY (id)
);
```

## Prisma (próximos passos)

- Converter para models no pacote `packages/database` seguindo [`docs/PADRAO_PRISMA.md`](../../docs/PADRAO_PRISMA.md).
- Preservar nomes físicos com `@@map` / `@map`.
