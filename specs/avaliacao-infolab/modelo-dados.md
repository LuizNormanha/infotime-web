# Modelo de dados — avaliacao-infolab

## Origem

Arquivos `.tll` em `migration-source/infotime-migration/avaliacao-infolab/database/`.

## Tabelas


### `avaliacao.tll`

```sql
-- Tabela: avaliacao
-- Entidade: avaliacao-infolab
-- Origem: MySQL → PostgreSQL (schema liga_infotime)

-- liga_infotime.avaliacao  (origem MySQL: avaliacao)
CREATE TABLE avaliacao (
  id_avaliacao bigint GENERATED BY DEFAULT AS IDENTITY NOT NULL,
  id_tenacidade bigint DEFAULT NULL,
  nota smallint DEFAULT NULL,
  nota2 smallint DEFAULT NULL,
  nota3 smallint DEFAULT NULL,
  nota4 smallint DEFAULT NULL,
  nota5 smallint DEFAULT NULL,
  data_hora timestamp without time zone DEFAULT NULL,
  usuario varchar(50) DEFAULT NULL,
  cnpj varchar(40) DEFAULT NULL,
  resposta1 varchar(255) DEFAULT NULL,
  resposta2 varchar(255) DEFAULT NULL,
  PRIMARY KEY (id_avaliacao)
);
```

### `info_lab_acesso.tll`

```sql
-- Tabela: info_lab_acesso
-- Entidade: avaliacao-infolab
-- Origem: MySQL → PostgreSQL (schema liga_infotime)

-- liga_infotime.info_lab_acesso  (origem MySQL: infolabacesso)
CREATE TABLE info_lab_acesso (
  id_info_lab_acesso bigint GENERATED BY DEFAULT AS IDENTITY NOT NULL,
  id_tenacidade bigint DEFAULT NULL,
  id_usuario_auditoria bigint DEFAULT NULL,
  cnpj varchar(20) DEFAULT NULL,
  usuario varchar(20) DEFAULT NULL,
  data_hora timestamp without time zone NULL DEFAULT NULL,
  endereco_ip_auditoria varchar(50) DEFAULT NULL,
  nome_aplicacao_auditoria varchar(255) DEFAULT NULL,
  PRIMARY KEY (id_info_lab_acesso)
);
```

### `info_lab_noticias.tll`

```sql
-- Tabela: info_lab_noticias
-- Entidade: avaliacao-infolab
-- Origem: MySQL → PostgreSQL (schema liga_infotime)

-- liga_infotime.info_lab_noticias  (origem MySQL: infolabnoticias)
CREATE TABLE info_lab_noticias (
  id_info_lab_noticias bigint GENERATED BY DEFAULT AS IDENTITY NOT NULL,
  id_tenacidade bigint DEFAULT NULL,
  id_usuario_auditoria bigint DEFAULT NULL,
  titulo varchar(255) DEFAULT NULL,
  resumo varchar(255) DEFAULT NULL,
  data_hora timestamp without time zone NULL DEFAULT NULL,
  mensagem text,
  urgente char(1) DEFAULT NULL,
  ativo char(1) DEFAULT NULL,
  endereco_ip_auditoria varchar(50) DEFAULT NULL,
  nome_aplicacao_auditoria varchar(255) DEFAULT NULL,
  PRIMARY KEY (id_info_lab_noticias)
);
```

## Prisma (próximos passos)

- Converter para models no pacote `packages/database` seguindo [`docs/PADRAO_PRISMA.md`](../../docs/PADRAO_PRISMA.md).
- Preservar nomes físicos com `@@map` / `@map`.
