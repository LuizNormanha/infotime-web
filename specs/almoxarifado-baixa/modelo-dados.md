# Modelo de dados — almoxarifado-baixa

## Origem

Arquivos `.tll` em `migration-source/infotime-migration/almoxarifado-baixa/database/`.

## Tabelas


### `almoxarifado_baixa.tll`

```sql
-- Tabela: almoxarifado_baixa
-- Entidade: almoxarifado-baixa
-- Origem: MySQL → PostgreSQL (schema liga_infotime)

-- liga_infotime.almoxarifado_baixa  (origem MySQL: almoxarifadobaixa)
CREATE TABLE almoxarifado_baixa (
  id_almoxarifado_baixa bigint GENERATED BY DEFAULT AS IDENTITY NOT NULL,
  id_tenacidade bigint DEFAULT NULL,
  id_almoxarifado_origem bigint DEFAULT NULL,
  id_almoxarifado_motivo_baixa bigint DEFAULT NULL,
  id_usuario_baixa bigint DEFAULT NULL,
  id_usuario_auditoria bigint DEFAULT NULL,
  data_hora_baixa timestamp without time zone DEFAULT NULL,
  observacoes text,
  endereco_ip_auditoria varchar(50) DEFAULT NULL,
  nome_aplicacao_auditoria varchar(255) DEFAULT NULL,
  PRIMARY KEY (id_almoxarifado_baixa)
);
```

### `almoxarifado_baixa_produto.tll`

```sql
-- Tabela: almoxarifado_baixa_produto
-- Entidade: almoxarifado-baixa
-- Origem: MySQL → PostgreSQL (schema liga_infotime)

-- liga_infotime.almoxarifado_baixa_produto  (origem MySQL: almoxarifadobaixaproduto)
CREATE TABLE almoxarifado_baixa_produto (
  id_almoxarifado_baixa_produto bigint GENERATED BY DEFAULT AS IDENTITY NOT NULL,
  id_tenacidade bigint DEFAULT NULL,
  id_almoxarifado_origem bigint DEFAULT NULL,
  id_almoxarifado_produto bigint NOT NULL,
  id_usuario_baixa bigint DEFAULT NULL,
  id_almoxarifado_motivo_baixa bigint DEFAULT NULL,
  id_almoxarifado_produto_estoque bigint DEFAULT NULL,
  id_usuario_auditoria bigint DEFAULT NULL,
  lote varchar(50) DEFAULT NULL,
  data_hora_baixa timestamp without time zone DEFAULT NULL,
  quantidade decimal(10,2) NOT NULL,
  observacoes text,
  endereco_ip_auditoria varchar(50) DEFAULT NULL,
  nome_aplicacao_auditoria varchar(255) DEFAULT NULL,
  PRIMARY KEY (id_almoxarifado_baixa_produto)
);
```

### `almoxarifado_motivo_baixa.tll`

```sql
-- Tabela: almoxarifado_motivo_baixa
-- Entidade: almoxarifado-baixa
-- Origem: MySQL → PostgreSQL (schema liga_infotime)

-- liga_infotime.almoxarifado_motivo_baixa  (origem MySQL: almoxarifadomotivobaixa)
CREATE TABLE almoxarifado_motivo_baixa (
  id_almoxarifado_motivo_baixa bigint GENERATED BY DEFAULT AS IDENTITY NOT NULL,
  sigla varchar(30) DEFAULT NULL,
  descricao varchar(255) DEFAULT NULL,
  PRIMARY KEY (id_almoxarifado_motivo_baixa)
);
```

## Prisma (próximos passos)

- Converter para models no pacote `packages/database` seguindo [`docs/PADRAO_PRISMA.md`](../../docs/PADRAO_PRISMA.md).
- Preservar nomes físicos com `@@map` / `@map`.
