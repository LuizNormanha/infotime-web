/**
 * Padrão visual para campos cujo valor é gerado pelo banco (auto incremento / serial).
 * Usar em `InputText` com `disabled`: combine com `liga-formulario-input`.
 */
export const LIGA_FORM_CLASS_INPUT_CHAVE_AUTOMATICA =
  "liga-formulario-input liga-formulario-input--chave-automatica";

/**
 * Campos de auditoria / rastreio preenchidos pelo sistema. Sempre `InputText` com `disabled`.
 */
export const LIGA_FORM_CLASS_INPUT_AUDITORIA =
  "liga-formulario-input liga-formulario-input--auditoria";
