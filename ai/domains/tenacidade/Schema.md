# Schema — tenacidade

Estrutura física e relacionamentos: consulte `api/prisma/schema.prisma` (modelos `infolab_*`) e as migrations do repositório.
Não inventar colunas, FKs ou cardinalidade fora do que está versionado no schema.

_Conteúdo gerado na migração de `domain/` → `ai/domains/`; enriqueça com detalhes do modelo quando necessário._
